step 1 อ่าน ไฟล์ @docs/PRD.md จากนั้นให้ทำการวิเคราะห์และแบ่งงานออกมา เป็น task เก็บไว้ใน folder Tasks โดยแยกแต่ละ task ย่อยเป็น 1 ต่อ 1 (ยังไม่ได้สร้าง Folder )
step 2 ให้ทำ init project for react (vite js ) ให้เรียบร้อย และใช้ css Tailwind



เริ่มทำ @Tasks/FR-1-authentication-sso.md เลย จนทั้งหมด ใน folder tasks ทำเป็น dummy data เป็น local dabase เอาทีเดียวผ่าน เสร็จแล้วบอก